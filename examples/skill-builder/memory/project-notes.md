# Project notes

Seed memory for the skill-builder session. Update this file as standing context
evolves; it is auto-discovered from the workspace root.

## Standing preferences

- Default deliverable: a full, installable Tangent Configuration Bundle packaged
  as `<id>.zip`, not a loose skill directory.
- Skill descriptions are written in the third person and state both WHAT the
  skill does and WHEN to use it.
- Prefer one default approach with an escape hatch over listing many
  interchangeable options.

## Working notes

- (Record the skills authored so far, decided trigger scenarios, and rejected
  directions here so the context survives across turns.)
